kind = "web"
previewPath = "/"
title = "Dairy Management Dashboard"
version = "1.0.0"
id = "artifacts/dairy-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22822

[services.development]
run = "pnpm --filter @workspace/dairy-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/dairy-dashboard", "run", "build" ]
publicDir = "artifacts/dairy-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22822"
BASE_PATH = "/"
